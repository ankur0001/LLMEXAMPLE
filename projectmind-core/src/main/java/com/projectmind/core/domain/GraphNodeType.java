package com.projectmind.core.domain;

/**
 * Types of nodes in the project knowledge graph.
 */
public enum GraphNodeType {
    PACKAGE,
    CLASS,
    INTERFACE,
    ENUM,
    METHOD,
    CONFIGURATION,
    ENTITY,
    CONTROLLER,
    SERVICE,
    REPOSITORY,
    FILTER,
    DATABASE,
    MODULE
}
